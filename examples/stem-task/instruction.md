Write your answer to `/app/answer.json` as a JSON object with exactly one top-level key, `"solutions"`. Do not write any files other than `/app/answer.json`.

Determine all integers `n > 1` such that `(2^n + 1) / n^2` is an integer.

Additional requirements:

- `"solutions"` must be a JSON array of integers.
- The array must contain every valid `n` exactly once.
- The array must be sorted in strictly increasing order.
